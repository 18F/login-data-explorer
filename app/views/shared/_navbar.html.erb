<nav class="navbar navbar-default">
  <div class='container'>
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Admin console", root_url, class: 'navbar-brand' %>
    </div>
    <% if admin_signed_in? %>
      <ul class='nav navbar-nav navbar-left'>
        <li><%= link_to "Blazer", blazer_url %></li>
        <li><%= link_to "User lookup", user_lookup_url %></li>
      </ul>
      <%= button_to 'Sign out',
          auth_sign_out_path,
          method: :delete,
          class: 'btn btn-primary',
          form_class: 'navbar-form navbar-right'
          %>
    <% else %>
      <div class='navbar-form navbar-right'>
        <%= link_to 'Sign in', '/auth/logindotgov', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</nav>
